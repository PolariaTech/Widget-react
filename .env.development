# Valores de desarrollo local (npm run dev / build sin --mode). Los mismos
# que ya estaban hardcodeados en config.ts antes de Fase 2 — no son secretos
# (el preset de Cloudinary es unsigned y el webhook no tiene autenticación
# todavía, ver docs/MATEO_SUPPORT_DOC.md), así que este archivo sí se versiona.
VITE_N8N_WEBHOOK_URL=https://polariatech.app.n8n.cloud/webhook/test-mateo-support
VITE_CLOUDINARY_CLOUD_NAME=ujssaxx6
VITE_CLOUDINARY_UPLOAD_PRESET=mateo_test_unsigned
